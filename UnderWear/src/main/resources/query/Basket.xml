<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.client.member.basket.dao.BasketDao">
	<select id="basketList" parameterType="String" resultType="basket">
		select b_no, img_01, p_name, pr_01, b_count, pr_01*b_count as total 
		from basket inner join product on basket.p_code = product.p_code 
		inner join image on image.p_code = product.p_code
		inner join price on price.pr_no = product.pr_no
	</select>
</mapper>