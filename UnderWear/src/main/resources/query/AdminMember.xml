<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.client.admin.member.dao.AdminMemberDao">
	<select id="memberList" resultType="member"> 
       /*마스킹 하기*/
 	   select idx, pinno, joindate,
       regexp_replace(userid,'.','*',4) as userid,
       regexp_replace(username,'.','*',2) as username,
       regexp_replace(phone,'(\d{3})\-(\d{3,4})\-(\d{4})','\1-****-\3') as phone,
       concat('*****',substr(email,6,length(email))) as email
       from spring_member
   </select>
</mapper>